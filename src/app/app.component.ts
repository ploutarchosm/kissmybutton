import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
    <div class="container bg-light">
      <app-body></app-body>
    </div>
  `
})
export class AppComponent {}
